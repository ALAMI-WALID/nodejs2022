<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title1 %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <h1>
        <%= title1 %>
    </h1>
    <ul>
        <% users.forEach(user => { %>
            <li>
                <%= user.name %> a
                    <%= user.age %> ans et son compte
                        <% if (user.active) {%>
                            est actif
                            <% } else { %>
                                n'est pas actif
                                <% } %>
            </li>
            <% }); %>

    </ul>

    <!-- api -->
    <h1>
        <%= title2 %>
    </h1>
    <ul>
        <% pokemon.forEach(poke => { %>
            <li>
                <%= poke.name %>
            </li>
            <% }); %>

    </ul>

</body>

</html>